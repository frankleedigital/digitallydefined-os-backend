# Supabase project configuration
# Hermes Optimization Loop — scheduled functions (daily loop + weekly report).

project_id = "dijjlppdljpcgyoakdnq"

[functions.optimization-loop]
verify_jwt = false
# Daily optimization loop: cluster + persist optimization_signals → user_clusters
schedule = "0 5 * * *"

[functions.optimization-loop-weekly]
verify_jwt = false
# Weekly report: invoke optimization-loop with { "mode": "weekly" }
schedule = "0 6 * * 1"

# Analytics pipeline: ingestion (open to website tracker) + reporting
# (protected by x-api-key inside the function itself).
[functions.analytics]
verify_jwt = false

# Notion sync — push new leads / quiz results / contact messages to Notion.
# Runs every 6 hours. Dry-run unless NOTION_LIVE_MODE=true.
[functions.notion-sync]
verify_jwt = false
schedule = "0 */6 * * *"